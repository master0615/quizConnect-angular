import { ShowErrorsModule } from './show-errors.module';

describe('ShowErrorsModule', () => {
	let showErrorsModule: ShowErrorsModule;

	beforeEach(() => {
		showErrorsModule  
	});

	it('should create an instance', () => {
		expect(showErrorsModule).toBeTruthy();
	});
});
